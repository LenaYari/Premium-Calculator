package com.proofit.task;

import java.math.MathContext;
import java.math.RoundingMode;

public class Utils {

    public static final MathContext MATH_CONTEXT = new MathContext(3, RoundingMode.HALF_UP);
}
